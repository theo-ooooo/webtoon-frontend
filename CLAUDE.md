# Frontend

Next.js 16 (App Router) + React 19 + TypeScript + Tailwind 4

상세: `docs/frontend.md`, DB/API: `docs/architecture.md`

```bash
npm run dev      # 개발서버 (localhost:3000)
npm run build    # 빌드
npm run lint     # 린트
```
